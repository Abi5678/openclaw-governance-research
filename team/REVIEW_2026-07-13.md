# Review — 2026-07-13

**Verdict:** REQUEST_CHANGES

## Findings
1. **Benchmark cardinality mismatch:** `paper/07_evaluation.md:7-20,35-49` still presents the composition benchmark as 10 scenarios and claims `openclaw_ordered` is `10/10`, but the checked-in toy benchmark snapshot `results/composition_benchmark.csv:2-10` only contains 9 scenarios per strategy (no `delegated_remediation_conflict`). The regenerated summary from `results/generate_plots.py` therefore reports 9-scenario counts, not the paper's 10-scenario claim.
2. **Publication summary is stale:** `results/EVALUATION_SUMMARY.md:9-81` does not reproduce from the current CSV inputs. Running `python results/generate_plots.py` against the workspace produces a different composition-benchmark section (9 scenarios, different latency stats, different key findings), so the published summary is not grounded in the current data snapshot.
3. **Paper drift across sections:** `paper/02_motivation.md:29-43` now frames the motivation around 9 modes with `policy_laundering` as the ninth item and says ordered composition succeeds on 9 scenarios, while `paper/07_evaluation.md:7-20` still describes 10 evaluated scenarios and includes `delegated_remediation_conflict`. Readers will see two incompatible benchmark counts in the manuscript.

## Required fixes
1. Regenerate or realign `results/composition_benchmark.csv`, then refresh `results/EVALUATION_SUMMARY.md` from the same source of truth.
2. Make `paper/02_motivation.md` and `paper/07_evaluation.md` describe the same scenario set/count, or explicitly label one section as pending/non-published.
3. Re-run the summary generator and confirm the committed summary matches the generator output before merging.

## Verification commands for builder
- `python experiments/composition_benchmark.py --csv /tmp/composition_regen.csv`
- `python results/generate_plots.py > /tmp/openclaw_eval_summary.txt && diff -u results/EVALUATION_SUMMARY.md /tmp/openclaw_eval_summary.txt`
- `python - <<'PY'
import csv
rows = list(csv.DictReader(open('results/composition_benchmark.csv', newline='')))
scenarios = sorted({r['scenario'] for r in rows})
print(len(scenarios), scenarios)
PY`
