# OpenClaw Review — 2026-06-30

Verdict: REQUEST_CHANGES

## Findings
1. `paper/main.md:7-18` still says the project formalizes **8** composition failure modes, but `paper/main.md:62-74` and `paper/07_evaluation.md:7-17` now describe **9** scenarios. The abstract/contributions are out of sync with the evaluation section.
2. `paper/07_evaluation.md:25-62` claims latency is measured "over 100 runs" and hard-codes mean latency values for the new table, but the benchmark harness computes one timing per scenario per strategy. Fresh runs in this workspace and the checked-in CSVs do not match the reported numbers (`results/composition_benchmark_real_adapters.csv:71-72`).
3. `experiments/composition_benchmark_real_adapters.py:502-508` exports only `trace_completeness`, not `trace_refs`, so the audit-reconstruction / skip-marker claim in Section 7 is not reproducible from the real-adapter artifact alone. The CSV row for `audit_reconstruction` confirms the field is absent (`results/composition_benchmark_real_adapters.csv:1,72`).

## Required fixes
- Sync `paper/main.md` abstract + contribution bullets to the 9-scenario framing.
- Reconcile `paper/07_evaluation.md` latency methodology and numbers with the actual benchmark harness / CSV outputs.
- Either export `trace_refs` from the real-adapter benchmark or narrow the paper wording so it no longer implies the skip markers are recoverable from the tracked artifact.

## Nits
- `team/STATE.md` table formatting had extra leading pipes; I normalized it while updating the review state.

## Verification commands to re-run
- `python3 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3.10 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3 experiments/governance_service_benchmark.py --csv results/governance_service_benchmark.csv`
- `git diff --check`
