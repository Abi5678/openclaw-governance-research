# Builder Handoff — 2026-07-16

## Objective
Tighten the policy-laundering / provenance-retention increment so the benchmark and related-work sections make the laundering boundary testable without recentering the paper on prompt-injection defense.

## Files changed
- `docs/evaluation-plan.md` — clarified S10 so provenance retention is recoverable after sanitization and adapter/summarizer lineage stays visible in the trace.
- `docs/related-work.md` — expanded provenance-preserving precedent to include AgentArmor, AuthGraph, VIGIL, and trace-based assurance while preserving the composition-layer novelty claim.
- `paper/08_related_work.md` — mirrored the provenance-preserving precedent and kept OpenClaw-Govern framed as the composition layer.
- `paper/07_evaluation.md` — added a brief note that laundering is evaluated separately on provenance retention, not just verdict accuracy.
- `team/STATE.md` — marked the builder run as in progress during execution.

## Validation output
`git diff --check; printf 'EXIT:%s\n' "$?"`

```text
warning: in the working copy of 'results/composition_benchmark_real_adapters.csv', CRLF will be replaced by LF the next time Git touches it
EXIT:0
```

## Acceptance criteria check
- [x] `docs/evaluation-plan.md` keeps S10 as the laundering case and makes provenance retention recoverability explicit.
- [x] `paper/08_related_work.md` and `docs/related-work.md` mention AgentArmor/AuthGraph/VIGIL/trace-based assurance as provenance-preserving precedent while keeping OpenClaw-Govern framed as a composition layer.
- [x] `paper/07_evaluation.md` stays numerically consistent with the checked-in CSV/summary; no benchmark numbers or tables were changed.
- [x] No benchmark/export files were modified, so no additional `provenance_retained` plumbing was needed in this increment.
- [x] The manuscript now frames laundering as a separate provenance-retention evaluation concern rather than the paper’s core novelty.

## Known issues
- `git diff --check` reports a pre-existing CRLF warning for `results/composition_benchmark_real_adapters.csv`; it did not block validation and no CSV content was changed in this increment.
- The known latency-table drift in `paper/07_evaluation.md` remains untouched by design.

## Suggested review focus for reviewer
Please verify the new prose keeps the laundering case clearly subordinate to the composition-layer contribution and that no paper numbers shifted accidentally.
