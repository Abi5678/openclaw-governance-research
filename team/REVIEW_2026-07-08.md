# Review — 2026-07-08

**Verdict:** REQUEST_CHANGES

## Findings
1. **Blocking — the summary generator is not yet reproducible from the current workspace snapshot.** `results/generate_plots.py:83-145` is supposed to derive the paper-ready findings from the current CSVs, but direct execution in this workspace (`python3 -B results/generate_plots.py`) still prints the stale 9-scenario / old-latency summary rather than the checked-in 10-scenario text. Until that mismatch is resolved, the regeneration claim is not externally reproducible.
2. **Non-blocking — the real-adapter CSV still triggers line-ending noise.** `git diff --check` continues to warn that `results/composition_benchmark_real_adapters.csv` will be normalized from CRLF to LF on the next Git touch.
3. **Non-blocking — the evaluation prose is slightly broader than the data story.** `paper/07_evaluation.md:33` and `results/EVALUATION_SUMMARY.md:73-76,145` describe the priority baseline / delegated-remediation conflict in broad terms; the numeric tables are correct, but the wording could be tightened to avoid implying more conflict misses than the current snapshot shows.

## Required fixes
- Re-run and/or repair `results/generate_plots.py` so a clean execution path produces the same numbers and narrative as `results/EVALUATION_SUMMARY.md` and `paper/07_evaluation.md` from the current CSV snapshot.
- Re-run the full summary generation after fixing the above and confirm the printed report, paper table, and checked-in summary are all aligned.

## Nits
- Normalize `results/composition_benchmark_real_adapters.csv` to LF to remove diff noise.
- Tighten the conflict-language in `paper/07_evaluation.md` / `results/EVALUATION_SUMMARY.md` so it matches the current single-miss baseline behavior exactly.

## Verification commands
- `python3 results/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3 results/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3 -B results/generate_plots.py`
- `git diff --check`
