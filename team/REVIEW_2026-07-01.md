# OpenClaw Review — 2026-07-01

Verdict: REQUEST_CHANGES

## Findings
1. `results/composition_benchmark_real_adapters.csv:65-73` no longer matches the latency table in `paper/07_evaluation.md:48-66` or `results/EVALUATION_SUMMARY.md:135-140`. The regenerated CSV shows `openclaw_ordered` at 0.0086/0.0076/0.0136 ms and `none` at 0.0008/0.0004/0.0037 ms, but the draft and summary still report 0.0055/0.0049/0.0084 ms and a 0.0050 ms mean overhead. Those numbers are not reproducible from the tracked artifact.
2. `paper/07_evaluation.md:66` still says real-adapter validation "does not export skip markers," but `results/composition_benchmark_real_adapters.csv:72` now includes `trace_refs` with explicit `:skipped` markers for the `audit_reconstruction` scenario. The public summary already claims `trace_refs` export, so the manuscript and summary now disagree on the same evidence.
3. `results/EVALUATION_SUMMARY.md:142-150` is only partially updated: it advertises exported `trace_refs`, but its latency table remains stale relative to the regenerated CSV. This makes the public evaluation summary internally inconsistent with the benchmark artifact.

## Required fixes
- Refresh `paper/07_evaluation.md` latency text/table so every reported number is computed from the current real-adapter CSV.
- Update the `paper/07_evaluation.md` conflict-resolution paragraph to reflect that the real-adapter export now includes `trace_refs` and skip markers.
- Reconcile `results/EVALUATION_SUMMARY.md` with the regenerated CSV, including the baseline/ordered latency values and the stated absolute overhead.

## Nits
- `results/composition_benchmark_real_adapters.csv` still emits a CRLF warning under `git diff --check`; normalize line endings to avoid churn.

## Verification commands to re-run
- `python3 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3 experiments/governance_service_benchmark.py --csv results/governance_service_benchmark.csv`
- `git diff --check`
