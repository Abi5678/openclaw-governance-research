# Review — 2026-07-06

**Verdict:** REQUEST_CHANGES

## Findings
1. **Blocking — stale plot/summary source.** `results/generate_plots.py:14-18,48-67` still reads `results/composition_benchmark.csv`, which is the pre-change 9-scenario artifact. Re-running the script today produces 9-scenario counts (`1/9`, `4/9`, `8/9`, etc.), so the paper-ready summary/plots are not reproducible from the current benchmark code.
2. **Blocking — checked-in toy benchmark CSV is out of date.** `results/composition_benchmark.csv:1-83` does not include `delegated_remediation_conflict`, so the repository still carries an old benchmark export while the manuscript and real-adapter outputs now describe a 10-scenario benchmark. This is the root cause of the stale summary output above.
3. **Non-blocking — line-ending noise.** `results/composition_benchmark_real_adapters.csv` still triggers the `git diff --check` CRLF/LF warning. It does not invalidate the numbers, but normalizing to LF will avoid churn and future diff noise.

## Required fixes
- Regenerate or replace `results/composition_benchmark.csv` with the 10-scenario benchmark export.
- Update `results/generate_plots.py` so the paper-ready summary reads the current benchmark artifact(s) and emits the 10-scenario results.
- Re-run the benchmark/summary generation after the above changes and confirm the regenerated output matches the manuscript.

## Nits
- Consider adding a brief note in the summary if the real-adapter table intentionally reports only a subset of strategies.

## Verification commands
- `python3 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3 results/generate_plots.py`
- `python3.10 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `git diff --check`
