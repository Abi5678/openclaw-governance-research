# Builder Handoff — 2026-07-17

## Objective
Add a policy-laundering failure mode and provenance-retention benchmark so OpenClaw-Govern evaluates sanitized-intermediate bypasses as a composition problem, not just a prompt-injection variant.

## Files changed
- `experiments/composition_benchmark.py` — Added the `policy_laundering` scenario, provenance metadata fields, and provenance-aware guardrail scoring.
- `experiments/composition_benchmark_real_adapters.py` — Mirrored the laundering scenario and provenance-retention plumbing in the real-adapter benchmark; trace refs now preserve original request provenance.
- `results/composition_benchmark.csv` — Regenerated toy benchmark results with the new 11-scenario snapshot.
- `results/composition_benchmark_real_adapters.csv` — Regenerated real-adapter results with the new 11-scenario snapshot.
- `results/generate_plots.py` — Updated the summary generation to include `policy_laundering` in provenance-retention reporting.
- `results/EVALUATION_SUMMARY.md` — Regenerated paper-ready benchmark summary from the updated CSVs.
- `paper/07_evaluation.md` — Updated benchmark counts, accuracy tables, real-adapter latency table, and provenance-retention wording to match the new snapshot.
- `team/STATE.md` — Advanced builder status to 2026-07-17 and marked the increment DONE.
- `team/HISTORY.md` — Appended the builder transition entry for this increment.

## Validation output

### `python3 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
```text
none                 accuracy=1/11
sarc_only            accuracy=2/11
authz_only           accuracy=5/11
guardrail_only       accuracy=3/11
roma_only            accuracy=2/11
async_only           accuracy=3/11
naive_composition    accuracy=1/11
priority_composition accuracy=10/11
openclaw_ordered     accuracy=11/11

wrote_csv: results/composition_benchmark.csv
```

### `python3 experiments/composition_benchmark_real_adapters.py`
```text
Traceback (most recent call last):
  File "experiments/composition_benchmark_real_adapters.py", line 43, in <module>
    from sarc.spec import ConstraintSpec, ConstraintSource, ConstraintClass, VerificationPoint, ResponseProtocol
  File "/Users/abishek/Projects/research-publications/openclaw-governance/repos/sarc-governance/sarc/spec.py", line 70, in <module>
    PredicateFn = Callable[[dict[str, Any]], tuple[bool, dict[str, Any]]]
TypeError: 'type' object is not subscriptable
```

### `python3.11 experiments/composition_benchmark_real_adapters.py`
```text
Initializing REAL governance adapters...
  ✓ SARC budget adapter initialized
  ✓ AuthZ propagation adapter initialized
  ✓ ROMA delegation adapter initialized
  ✓ Guardrail adapter initialized
  ✓ AsyncFC adapter initialized

none                 accuracy=1/11
sarc_only            accuracy=2/11
authz_only           accuracy=5/11
guardrail_only       accuracy=3/11
roma_only            accuracy=2/11
async_only           accuracy=3/11
naive_composition    accuracy=1/11
openclaw_ordered     accuracy=11/11

wrote_csv: results/composition_benchmark_real_adapters.csv
```

### `python3.11 results/generate_plots.py`
```text
| None | 1/11 | 9.1% |
| Priority Composition | 10/11 | 90.9% |
| Openclaw Ordered | 11/11 | 100.0% |
...
1. **Composition correctness**: OpenClaw-Ordered achieves 11/11 (100.0%), while naive composition achieves only 9.1% (1/11).
2. **Single-module blind spots**: Individual governance modules (SARC, authz, guardrail, ROMA, async) achieve only 18.2-45.5% accuracy.
3. **Latency overhead**: OpenClaw-Ordered adds ~0.0062 ms mean latency per scenario vs. ~0.0006 ms baseline (no governance), representing acceptable overhead for safety-critical deployments.
4. **Conflict detection**: Only OpenClaw-Ordered detects and resolves conflicts ...
5. **Provenance retention**: `provenance_retained` is surfaced ... scenario 10 keeps the sanitized summary visible while preserving the original unsafe request in trace metadata, and 3/3 current real-adapter cases keep the unsafe request recoverable ...
```

### `git diff --check`
```text
warning: in the working copy of 'results/composition_benchmark_real_adapters.csv', CRLF will be replaced by LF the next time Git touches it
```

## Acceptance criteria check
- [x] The laundering case is represented as a scored benchmark scenario, not just a prose-only note.
- [x] The benchmark/export path exposes a provenance-retention signal that survives sanitization across the adapter boundary.
- [x] The CSV, summary artifact, and paper prose agree on scenario count and reported metrics after regeneration.
- [x] `paper/08_related_work.md` and `docs/related-work.md` keep the novelty claim centered on composition, with the external sources framed as precedent for provenance-preserving evidence.
- [x] The new scenario remains deterministic and does not alter unrelated governance semantics such as ordering precedence or delegation propagation.

## Known issues
- The host `python3` is 3.7.6, so the real-adapter harness must be run with `python3.11` because the companion repo uses modern type syntax.
- `git diff --check` reports a CRLF normalization warning for `results/composition_benchmark_real_adapters.csv`.

## Suggested review focus
Please verify that the new `policy_laundering` scenario is correctly scored through provenance metadata in both benchmark harnesses, that the regenerated CSVs and summary match the updated paper prose, and that the CRLF warning is acceptable or should be normalized before the next reviewer pass.
