# OpenClaw Review — 2026-07-02

Verdict: REQUEST_CHANGES

## Findings
1. `paper/07_evaluation.md:48-66` is stale relative to the regenerated real-adapter CSV. The paper still reports `openclaw_ordered` at 0.0055 / 0.0049 / 0.0084 ms and claims the real-adapter path does not export skip markers, but the current tracked CSV export was just regenerated with `openclaw_ordered` at 0.0065 / 0.0052 / 0.0044 / 0.0931 / 0.0454 / 0.0086 / 0.0043 / 0.0047 ms and includes explicit `trace_refs` plus `:skipped` markers for `audit_reconstruction`.
2. `results/EVALUATION_SUMMARY.md:133-150` remains internally inconsistent with the current CSV artifact. It still lists the old real-adapter latency table and absolute overhead (+0.0050 ms), which no longer matches the regenerated benchmark output from `results/composition_benchmark_real_adapters.csv:65-73`.
3. The current benchmark run does not support the paper/summary claim that ordered real-adapter evaluation remains in a sub-0.01 ms regime. The regenerated CSV yields `openclaw_ordered` mean 0.0198 ms and p95 0.0931 ms, so the prose in both the paper and summary materially overstates the current measurements.

## Required fixes
- Refresh `paper/07_evaluation.md` so the real-adapter latency text/table is computed from the current CSV export.
- Update the `paper/07_evaluation.md` conflict-resolution paragraph to reflect that real-adapter validation now exports `trace_refs` and skip markers.
- Reconcile `results/EVALUATION_SUMMARY.md` with the regenerated CSV, including baseline/ordered latency values, p95, and absolute overhead.

## Nits
- `results/composition_benchmark_real_adapters.csv` still triggers a CRLF warning under `git diff --check`; normalize line endings to reduce churn.
- The latest related-work/AARM positioning commit appears consistent with its own acceptance criteria and does not need further text changes.

## Verification commands to re-run
- `python3 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3 experiments/governance_service_benchmark.py --csv results/governance_service_benchmark.csv`
- `git diff --check`
