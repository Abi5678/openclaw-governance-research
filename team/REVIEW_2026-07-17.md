# Review — 2026-07-17

## Verdict
REQUEST_CHANGES

## Findings
1. **Blocking — `results/EVALUATION_SUMMARY.md` is internally inconsistent and stale.** The latency table reports OpenClaw-Ordered at `0.0088 ms` mean / `+85.9%` relative overhead (`results/EVALUATION_SUMMARY.md:19-31`), but the "Key Findings" section says OpenClaw-Ordered adds `~0.0062 ms` over a `~0.0006 ms` baseline (`results/EVALUATION_SUMMARY.md:35-39`). Those numbers cannot all be true at once, and they do not match the regenerated benchmark output from `python3.11 results/generate_plots.py`.
2. **Blocking — `paper/07_evaluation.md` still reflects an older real-adapter snapshot.** Table 2 and the surrounding latency narrative (`paper/07_evaluation.md:53-71`) report OpenClaw-Ordered at `0.0068 ms` mean / `+0.0062 ms` overhead, while the current real-adapter CSV generated in this run reports a different latency profile. The paper, summary artifact, and CSV need to be regenerated together so the published claims match the evidence.
3. **Nit — provenance-retention counting is fragile in `results/generate_plots.py`.** The summary logic at `results/generate_plots.py:102-109` uses `row.get("provenance_retained", row.get("trace_refs"))`, which treats non-empty CSV strings such as `"False"` as truthy. It happens to work for the current all-true lineage rows, but it can silently overcount on future snapshots.

## Required fixes
1. Regenerate `results/EVALUATION_SUMMARY.md` from the current CSVs and make its latency numbers self-consistent.
2. Update `paper/07_evaluation.md` Table 2 and the surrounding text to match the regenerated real-adapter CSV / summary.
3. If you keep the provenance-retention summary, parse the CSV field explicitly instead of relying on string truthiness.

## Verification commands to re-run
- `python3.11 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3.11 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3.11 results/generate_plots.py`
- `git diff --check`

## Commit / push
- Not committed.
- Not pushed.
