# Review — 2026-07-16

## Verdict
REQUEST_CHANGES

## Findings
1. **Blocking — real-adapter latency table drift.** `paper/07_evaluation.md:52-68` still reports OpenClaw-Ordered at `0.0077 ms` mean / `+0.0072` ms overhead, but the checked-in real-adapter CSV and regenerated summary output currently land at about `0.0053 ms` mean / `+0.0048` ms overhead. That makes Table 2 inconsistent with the evidence it cites.
2. **Nit — stale summary metadata.** `results/EVALUATION_SUMMARY.md:1-5` still says `Last updated: 2026-07-06`, even though the summary content appears to reflect the current workspace snapshot. Refresh the metadata when regenerating the artifact.
3. **Nit — line-ending noise remains.** `git diff --check` still warns that `results/composition_benchmark_real_adapters.csv` will be normalized from CRLF to LF on the next Git touch. It does not block correctness, but it keeps the working tree noisy.

## Required fixes
1. Update `paper/07_evaluation.md` Table 2 and the surrounding narrative so they match the current real-adapter CSV / regenerated summary, or deliberately regenerate the CSV in the same increment so the paper and evidence agree.

## Verification commands to re-run
- `python3.11 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3.11 results/generate_plots.py`
- `git diff --check`

## Commit / push
- Not committed.
- Not pushed.