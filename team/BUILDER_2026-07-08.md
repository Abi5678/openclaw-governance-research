# Builder Increment — 2026-07-08

## Objective
Tighten the paper’s positioning of `delegated_remediation_conflict` so the 10-scenario benchmark is consistently framed as a composition problem spanning ROMA-style delegation, AARM-style interception, async serialization, and deterministic conflict arbitration.

## Files changed
- `paper/07_evaluation.md` — refreshed real-adapter latency table and real-adapter accuracy section to match the current CSV snapshot.
- `paper/08_related_work.md` — tightened the AARM / ROMA framing so the delegated-remediation case explicitly reads as composition across delegation, interception, and arbitration.
- `docs/evaluation-plan.md` — aligned the delegated-remediation scenario wording with the trace-lineage + dual-intervention framing.
- `docs/related-work.md` — mirrored the paper wording in the matrix notes so the composition-layer framing stays consistent.
- `results/EVALUATION_SUMMARY.md` — updated the checked-in summary tables and key findings to match the current real-adapter CSV values.
- `results/generate_plots.py` — removed hardcoded summary claims and derived the key findings from the CSV snapshots.

## Validation output

### `python3 results/generate_plots.py`
```text
COMPOSITION BENCHMARK - PAPER-READY SUMMARY

1. **Composition correctness**: OpenClaw-Ordered achieves 10/10 (100.0%), while naive composition achieves only 10.0% (1/10).
2. **Single-module blind spots**: Individual governance modules (SARC, authz, guardrail, ROMA, async) achieve only 20.0-50.0% accuracy.
3. **Latency overhead**: OpenClaw-Ordered adds ~0.0073 ms mean latency per scenario vs. ~0.0004 ms baseline (no governance), representing acceptable overhead for safety-critical deployments.
4. **Conflict detection**: Only OpenClaw-Ordered detects and resolves conflicts (e.g., THROTTLE vs. SERIALIZE in scenario 7 and ESCALATE vs. SERIALIZE in scenario 9); scenario 9 preserves interventions `escalate,serialize` with trace completeness 1.0000.

GOVERNANCE SERVICE BENCHMARK - PAPER-READY SUMMARY
- **Accuracy**: 7/7 (100%)
- **Detection Rate**: 5/5 (100%)
- **False Negative Rate**: 0/5 (0%)
- **False Positive Rate**: 0/2 (0%)
- **P50 Latency**: 0.0095 ms
- **P95 Latency**: 0.0182 ms
- **Trace Completeness**: 7/7 (100%)
```

### `git diff --check`
```text
warning: in the working copy of 'results/composition_benchmark_real_adapters.csv', CRLF will be replaced by LF the next time Git touches it
```

## Acceptance criteria check
- [x] `delegated_remediation_conflict` is described consistently across `paper/07_evaluation.md`, `docs/evaluation-plan.md`, `paper/08_related_work.md`, and `docs/related-work.md`.
- [x] The related-work section explicitly distinguishes the composition layer from SARC-only runtime enforcement and from delegation-only or telemetry-only systems.
- [x] The evaluation text preserves both interventions in the trace for `delegated_remediation_conflict` and states the expected final verdict unambiguously.
- [x] Evaluation-summary / plotting text now derives key findings from the current CSV snapshots instead of hardcoding stale latency claims.
- [x] The benchmark narrative remains centered on composition of heterogeneous runtime controls, not on any single control family.

## Known issues
- `results/composition_benchmark.csv` still reflects the older 9-scenario toy snapshot; I did not retarget that benchmark run because the queued increment was specifically about the current real-adapter summary drift.
- `git diff --check` reported only the existing CRLF normalization warning for `results/composition_benchmark_real_adapters.csv`.

## Suggested review focus
Please verify the new real-adapter latencies and the delegated-remediation wording in `paper/07_evaluation.md` / `results/EVALUATION_SUMMARY.md`, and confirm the summary generator now derives the key findings from CSV values instead of stale hardcoded numbers.
