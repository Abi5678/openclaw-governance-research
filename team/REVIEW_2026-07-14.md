# Review — 2026-07-14

Verdict: REQUEST_CHANGES

## Findings
1. **Toy composition benchmark still has only 9 scenarios, but the paper and summary now claim 10.** `paper/07_evaluation.md:7-20` and `results/EVALUATION_SUMMARY.md:13-25` describe `delegated_remediation_conflict` as part of the composition benchmark, yet `results/composition_benchmark.csv:1-82` still contains 9 scenarios per strategy and no `delegated_remediation_conflict` row. Running `python3 results/generate_plots.py` still prints the toy benchmark accuracy table as `Openclaw Ordered | 9/9 | 100.0%`, so the manuscript is ahead of the checked-in benchmark snapshot.
2. **The checked-in evaluation summary is not reproducible from the committed composition CSV.** `results/EVALUATION_SUMMARY.md:47-71` reports 10-scenario accuracy and latency values, but those figures are not derivable from `results/composition_benchmark.csv:1-82` as currently committed. This is the same drift in a different artifact: the summary and paper were updated before the benchmark CSV was regenerated.
3. **The provenance-retention addition is only validated on the real-adapter side, not in the toy benchmark export.** `results/generate_plots.py:101-108` derives `provenance_retained` from real-adapter `trace_refs`, which is fine as a proxy, but the top-level composition benchmark narrative in `paper/07_evaluation.md` and `results/EVALUATION_SUMMARY.md` presents the new laundering scenario as already part of the reported benchmark. That claim is not supported until the toy benchmark CSV is rebuilt.

## Required fixes
1. Regenerate `results/composition_benchmark.csv` from the updated toy benchmark harness so it includes `delegated_remediation_conflict` and the new 10-scenario counts, then rerun `python3 results/generate_plots.py` and sync `paper/07_evaluation.md` / `results/EVALUATION_SUMMARY.md` to the regenerated numbers.
2. If the toy CSV is intentionally staying at 9 scenarios for now, roll back the 10-scenario claims in `paper/07_evaluation.md` and `results/EVALUATION_SUMMARY.md` so the paper matches the committed benchmark snapshot.
3. After the benchmark snapshot is reconciled, rerun the generator and re-check that the toy composition table, latency table, and the real-adapter provenance-retention note all come from the same committed data revision.

## Verification commands to rerun
- `python3 results/generate_plots.py`
- `python3 - <<'PY'\nimport csv\nfor path in ['results/composition_benchmark.csv','results/composition_benchmark_real_adapters.csv']:\n    with open(path, newline='') as f:\n        rows=list(csv.DictReader(f))\n    print(path, len(rows), sorted(set(r['scenario'] for r in rows)))\nPY`
- `git diff --check`
