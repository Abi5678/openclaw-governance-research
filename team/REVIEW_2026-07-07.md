# Review — 2026-07-07

**Verdict:** REQUEST_CHANGES

## Findings
1. **Blocking — `results/generate_plots.py` still hardcodes stale benchmark numbers.** `results/generate_plots.py:69-73` prints the OpenClaw latency finding as `~0.0363 ms` vs `~0.0009 ms` baseline, but the CSV it actually reads (`results/composition_benchmark.csv`) currently reports about `0.0033 ms` vs `0.0006 ms`. The paper-ready summary is therefore not reproducible from the artifact the script consumes.
2. **Blocking — the real-adapter prose and CSV are out of sync in the current workspace.** `paper/07_evaluation.md:49-67` and `results/EVALUATION_SUMMARY.md:135-144` still describe the older real-adapter snapshot, while `results/composition_benchmark_real_adapters.csv:1-81` has been regenerated in this workspace with different latencies (for example, `openclaw_ordered` is now ~`0.0077 ms` mean, baseline ~`0.0005 ms`). The manuscript, summary, and CSV need to come from the same run.
3. **Non-blocking — wording/norms need a cleanup pass.** `paper/07_evaluation.md:33` says priority composition still “mis-resolves the conflict cases,” but the current benchmark only shows one miss (`throttle_vs_serialize_conflict`). Also `results/composition_benchmark_real_adapters.csv` still triggers the CRLF/LF diff-check warning.

## Required fixes
- Replace the hardcoded latency findings in `results/generate_plots.py` with values computed from the CSV it reads, or clearly label the text as snapshot-specific and regenerate it with the matching artifact.
- Regenerate `paper/07_evaluation.md` and `results/EVALUATION_SUMMARY.md` from the same `results/composition_benchmark_real_adapters.csv` snapshot, or restore the CSV to the snapshot the prose cites.
- Re-run the benchmark/summary generation after the above changes and confirm the tables and narrative agree.

## Nits
- Tighten the priority-composition wording to match the single observed miss.
- Normalize `results/composition_benchmark_real_adapters.csv` to LF to remove diff noise.

## Verification commands
- `python3 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3.12 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3 results/generate_plots.py`
- `git diff --check`
