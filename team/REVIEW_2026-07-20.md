# Review — 2026-07-20

## Verdict
REQUEST_CHANGES

## Findings
1. **Blocking — `paper/07_evaluation.md` is self-inconsistent and does not match the regenerated benchmark snapshot.** The latency narrative says OpenClaw-Ordered adds `0.0052ms` mean overhead (`paper/07_evaluation.md:53-57`), but Table 2 reports OpenClaw-Ordered at `0.0057` mean / `+0.0052` absolute overhead (`paper/07_evaluation.md:58-69`). The current `results/generate_plots.py` run over the checked-in CSVs reports `0.0066 ms` mean and `0.0055 ms` overhead for OpenClaw-Ordered, so the paper is stale relative to the data.
2. **Blocking — `results/EVALUATION_SUMMARY.md` is internally inconsistent and stale.** The summary table lists OpenClaw-Ordered at `0.0037 ms` mean / `+640.0%` relative overhead (`results/EVALUATION_SUMMARY.md:19-31`), but the Key Findings section says OpenClaw-Ordered adds `~0.0052 ms` over the baseline (`results/EVALUATION_SUMMARY.md:33-39`). The same file therefore reports two different latency claims, and neither matches the current benchmark output.
3. **Nit — provenance-retention aggregation in `results/generate_plots.py` is fragile.** The code at `results/generate_plots.py:102-109` counts `provenance_retained` using `row.get("provenance_retained", row.get("trace_refs"))`, so non-empty strings like `"False"` would be treated as truthy. The current data happen to be all `True` for the lineage-sensitive rows, but this will silently overcount in a future snapshot.

## Required fixes
1. Regenerate `results/EVALUATION_SUMMARY.md` and `paper/07_evaluation.md` from the same benchmark snapshot so the table, narrative, and CSV-derived values agree.
2. Keep the benchmark summary aligned to the current `results/composition_benchmark_real_adapters.csv` output before merging.
3. If provenance retention stays in the summary script, parse the CSV field explicitly instead of relying on string truthiness.

## Verification commands to re-run
- `python3.11 experiments/composition_benchmark.py --csv results/composition_benchmark.csv`
- `python3.11 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3.11 results/generate_plots.py`
- `git diff --check`

## Commit / push
- Not committed.
- Not pushed.