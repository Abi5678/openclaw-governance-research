# Builder Report — 2026-07-21

## Objective
Finalize the policy-laundering / provenance-retention increment so the paper treats sanitized-intermediate bypasses as a scored benchmark scenario, while keeping the novelty claim centered on composable runtime governance.

## Files changed
- `docs/evaluation-plan.md` — locked the laundering case as a scored benchmark row and defined `provenance_retention` as the auxiliary metric.
- `paper/07_evaluation.md` — refreshed the real-adapter latency table and narrative to match the current CSV snapshot.
- `paper/08_related_work.md` — added provenance-preserving evidence positioning for AgentArmor, AuthGraph, VIGIL, and trace-based assurance.
- `docs/related-work.md` — mirrored the provenance-preserving positioning note in the matrix.
- `experiments/composition_benchmark.py` — kept the laundering scenario and provenance-retention scoring in the toy benchmark path.
- `experiments/composition_benchmark_real_adapters.py` — mirrored the laundering scenario and provenance-retention plumbing in the real-adapter benchmark.
- `results/composition_benchmark.csv` — regenerated toy benchmark results for the current 11-scenario snapshot.
- `results/composition_benchmark_real_adapters.csv` — regenerated real-adapter results for the current 11-scenario snapshot.
- `results/EVALUATION_SUMMARY.md` — regenerated the paper-ready summary from the current CSV-backed benchmark outputs.
- `results/generate_plots.py` — expanded summary generation to report priority composition and provenance-retention findings.
- `team/STATE.md` — advanced builder status to 2026-07-21 and marked the increment complete.
- `team/HISTORY.md` — appended the builder transition entry for this increment.

## Validation output

### `python3 experiments/composition_benchmark.py`
```text
none                 accuracy=1/11
sarc_only            accuracy=2/11
authz_only           accuracy=5/11
guardrail_only       accuracy=3/11
roma_only            accuracy=2/11
async_only           accuracy=3/11
naive_composition    accuracy=1/11
priority_composition accuracy=10/11
openclaw_ordered     accuracy=11/11

Latency overhead (ms per scenario):
  none                 mean=0.0050, median=0.0004, p95=0.0256
  sarc_only            mean=0.0066, median=0.0032, p95=0.0193
  authz_only           mean=0.0035, median=0.0028, p95=0.0054
  guardrail_only       mean=0.0032, median=0.0028, p95=0.0045
  roma_only            mean=0.0187, median=0.0031, p95=0.0881
  async_only           mean=0.0035, median=0.0030, p95=0.0055
  naive_composition    mean=0.0021, median=0.0018, p95=0.0032
  priority_composition mean=0.0088, median=0.0076, p95=0.0138
  openclaw_ordered     mean=0.0094, median=0.0081, p95=0.0139

wrote_csv: results/composition_benchmark.csv
```

### `python3 experiments/composition_benchmark_real_adapters.py`
```text
Traceback (most recent call last):
  File "experiments/composition_benchmark_real_adapters.py", line 43, in <module>
    from sarc.spec import ConstraintSpec, ConstraintSource, ConstraintClass, VerificationPoint, ResponseProtocol
  File "/Users/abishek/Projects/research-publications/openclaw-governance/repos/sarc-governance/sarc/spec.py", line 70, in <module>
    PredicateFn = Callable[[dict[str, Any]], tuple[bool, dict[str, Any]]]
TypeError: 'type' object is not subscriptable
```

### `python3.11 experiments/composition_benchmark_real_adapters.py`
```text
Initializing REAL governance adapters...
  ✓ SARC budget adapter initialized
  ✓ AuthZ propagation adapter initialized
  ✓ ROMA delegation adapter initialized
  ✓ Guardrail adapter initialized
  ✓ AsyncFC adapter initialized

none                 accuracy=1/11
sarc_only            accuracy=2/11
authz_only           accuracy=5/11
guardrail_only       accuracy=3/11
roma_only            accuracy=2/11
async_only           accuracy=3/11
naive_composition    accuracy=1/11
openclaw_ordered     accuracy=11/11

Latency overhead (ms per scenario):
  none                 mean=0.0013, median=0.0008, p95=0.0034
  sarc_only            mean=0.0190, median=0.0118, p95=0.0449
  authz_only           mean=0.0134, median=0.0058, p95=0.0478
  guardrail_only       mean=0.0050, median=0.0040, p95=0.0089
  roma_only            mean=0.0039, median=0.0033, p95=0.0056
  async_only           mean=0.0076, median=0.0040, p95=0.0191
  naive_composition    mean=0.0417, median=0.0081, p95=0.1921
  openclaw_ordered     mean=0.1837, median=0.0182, p95=0.9203

wrote_csv: results/composition_benchmark_real_adapters.csv
```

### `python3.11 results/generate_plots.py`
```text
================================================================================
COMPOSITION BENCHMARK - PAPER-READY SUMMARY
================================================================================

## Accuracy by Strategy

| Strategy | Accuracy | % Correct |
|----------|----------|-----------|
| None | 1/11 | 9.1% |
| Sarc Only | 2/11 | 18.2% |
| Authz Only | 5/11 | 45.5% |
| Guardrail Only | 3/11 | 27.3% |
| Roma Only | 2/11 | 18.2% |
| Async Only | 3/11 | 27.3% |
| Naive Composition | 1/11 | 9.1% |
| Priority Composition | 10/11 | 90.9% |
| Openclaw Ordered | 11/11 | 100.0% |
...
1. **Composition correctness**: OpenClaw-Ordered achieves 11/11 (100.0%), while naive composition achieves only 9.1% (1/11).
2. **Single-module blind spots**: Individual governance modules (SARC, authz, guardrail, ROMA, async) achieve only 18.2-45.5% accuracy.
3. **Latency overhead**: OpenClaw-Ordered adds ~0.1824 ms mean latency per scenario vs. ~0.0013 ms baseline (no governance), representing acceptable overhead for safety-critical deployments.
4. **Conflict detection**: Only OpenClaw-Ordered detects and resolves conflicts (e.g., THROTTLE vs. SERIALIZE in scenario 7 and ESCALATE vs. SERIALIZE in scenario 9); scenario 9 preserves interventions `escalate,serialize` with trace completeness 1.0000.
5. **Provenance retention**: `provenance_retained` is surfaced for the lineage-sensitive audit scenarios; scenario 10 keeps the sanitized summary visible while preserving the original unsafe request in trace metadata, and 3/3 current real-adapter cases keep the unsafe request recoverable across the adapter / summary boundary via lineage-bearing trace refs.
```

### `git diff --check`
```text
warning: in the working copy of 'results/composition_benchmark_real_adapters.csv', CRLF will be replaced by LF the next time Git touches it
```

## Acceptance criteria check
- [x] The laundering case is treated as a scored benchmark scenario with an explicit provenance-retention metric.
- [x] The paper text distinguishes verdict correctness from provenance recovery and does not collapse the scenario into generic prompt-injection framing.
- [x] Related-work positioning stays composition-centered: the external systems are precedents for provenance/evidence, while OpenClaw-Govern is the runtime composition layer.
- [x] Any benchmark/export artifacts touched by the increment remain numerically consistent with the paper narrative.
- [x] The change does not alter ordering semantics, delegation propagation rules, or conflict arbitration behavior outside the laundering scenario.

## Known issues
- The host `python3` resolves to Python 3.7.6, so the real-adapter benchmark must be run with `python3.11` here.
- `git diff --check` reports a CRLF normalization warning for `results/composition_benchmark_real_adapters.csv`.
- `uv run` hit an unrelated metadata issue in the local environment, so `python3.11` was the reliable validation path.

## Suggested review focus for reviewer
- Confirm Table 2 in `paper/07_evaluation.md` matches the refreshed real-adapter CSV snapshot.
- Confirm the summary language still separates provenance retention from verdict correctness.
- Confirm the refreshed benchmark summary does not introduce any scope creep beyond the laundering/provenance increment.
