# Objective
Tighten the delegated-remediation composition position so the manuscript clearly frames Scenario 9 as ROMA-style delegation plus AARM-style interception plus async serialization/conflict arbitration, with XACML/OPA cited as arbitration-and-trace precedents rather than as the novelty itself.

# Background
- The top unchecked queue item already targets delegated-remediation positioning, so the next useful increment is prose alignment rather than new benchmark mechanics.
- `paper/08_related_work.md` already separates ROMA delegation, AARM interception, and path-based governance, but the delegated-remediation discussion should make the composition claim explicit in one place.
- `paper/07_evaluation.md` already reports `delegated_remediation_conflict` as `ESCALATE`, but the surrounding wording still risks sounding like a generic step-up case instead of a three-way composition case.
- `docs/related-work.md` is the clearest place to preserve the paper’s positioning logic in a compact matrix: ROMA for recursive delegation, AARM for action-layer interception, and OpenClaw-Govern for composition across modules.
- XACML combining algorithms and OPA decision logs are useful because they show deterministic arbitration and auditable decision records, but they remain narrower than recursive tool-agent governance.
- The novelty claim should stay centered on composition: ordered module execution, conflict resolution, delegation propagation, and unified traces.
- The most important trace detail in Scenario 9 is that both the step-up signal and the serialization signal remain visible even though the final verdict is `ESCALATE`.

# Proposed change
Update the following files in the paper/docs surface:

1. `paper/08_related_work.md`
   - Strengthen §8.2 and §8.5 so the text explicitly says ROMA supplies delegation structure and traceable recursion, AARM supplies interception/receipts, and OpenClaw-Govern adds the composition layer that orders and reconciles heterogeneous runtime controls.
   - Add a sentence that uses `delegated_remediation_conflict` as the concrete example of why delegation lineage, step-up approval, and async serialization must all remain visible together in the trace.
   - Keep the distinction clear that this is not a SARC-only framing.

2. `paper/07_evaluation.md`
   - Reword Scenario 9 and the conflict-resolution paragraph so the benchmark case is described as ROMA-style delegation lineage + AARM-style interception + async serialization/conflict arbitration.
   - Preserve the existing verdict (`ESCALATE`) and the trace-completeness claim.
   - Make it explicit that the composed trace records both the step-up and serialization interventions.

3. `docs/related-work.md`
   - Mirror the manuscript wording in the matrix and positioning notes.
   - Ensure the ROMA row emphasizes delegation/traceability, the AARM row emphasizes interception/receipts, and the OpenClaw-Govern notes emphasize deterministic arbitration across modules.
   - Mention XACML/OPA as precedents for deterministic combining and auditable decision records.

4. `team/QUEUE.md`
   - Refresh the selected queue item date to 2026-07-10 so builder sees this as the current handoff.

# Sources cited
- https://arxiv.org/abs/2602.01848 — accessed 2026-07-10 — ROMA’s recursive decomposition, role separation, and hierarchical traces; useful contrast for delegation structure without governance arbitration.
- https://arxiv.org/html/2602.09433v1 — accessed 2026-07-10 — AARM’s runtime interception, contextual evaluation, and tamper-evident receipts; useful contrast for action-layer enforcement without composition semantics.
- https://docs.oasis-open.org/xacml/xacml-3.0-combalgs/v1.0/xacml-3.0-combalgs-v1.0.html — accessed 2026-07-10 — XACML’s additional combining algorithms; useful precedent for deterministic arbitration.
- https://www.openpolicyagent.org/docs/management-decision-logs — accessed 2026-07-10 — OPA decision logs; useful precedent for auditable, traceable policy decisions.

# Acceptance criteria for builder
1. `paper/08_related_work.md` explicitly names the composition layer and does not let the delegated-remediation example read like SARC-only runtime enforcement.
2. `paper/07_evaluation.md` still reports `ESCALATE` for Scenario 9, but the prose clearly mentions ROMA-style delegation, AARM-style interception, and async serialization/conflict arbitration.
3. `docs/related-work.md` matches the manuscript’s positioning and distinguishes delegation, interception, and arbitration as separate antecedents of the OpenClaw-Govern composition layer.
4. `team/QUEUE.md` shows the same item as the top unchecked increment with a 2026-07-10 handoff marker.
5. No benchmark counts, verdicts, or latency figures change; only framing and trace-language alignment are updated.

# Out of scope
- Adding a new benchmark scenario.
- Changing experiment code or regenerating results.
- Revising the evaluation numbers or plots.
- Broadening the paper beyond the delegated-remediation positioning increment.
