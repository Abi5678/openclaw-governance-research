# Objective
Tighten the delegated-remediation composition positioning so the manuscript and handoff docs consistently frame Scenario 9 as ROMA-style delegation plus AARM-style interception plus async serialization/conflict arbitration, rather than as a SARC-only or authz-only example.

# Background
- The queue item is the delegated-remediation composition positioning increment selected from the 2026-07-08 backlog.
- Current paper language already names Scenario 9 (`delegated_remediation_conflict`) as step-up approval plus async serialization, but the surrounding prose still reads as a local conflict example rather than a three-way composition case.
- The related-work matrix already distinguishes ROMA delegation structure, AARM interception, and path-based governance, but the manuscript should make the cross-module composition claim explicit in the positioning text.
- The benchmark row for Scenario 9 is the best place to show that delegation lineage, step-up approval, and serialization remain visible in the unified trace even when the final verdict is ESCALATE.
- The main risk is drift between the benchmark description, related-work framing, and evaluation prose, which can make the novelty look like “SARC plus a conflict case” instead of a composition layer across heterogeneous runtime controls.
- Existing companion docs already contain the right ingredients; this increment is about synchronizing them around the composition thesis.

# Proposed change
Update the following files in the manuscript/docs surface:
1. `paper/08_related_work.md`
   - Strengthen §8.2/§8.5 with an explicit sentence that ROMA gives delegation structure, AARM gives interception/receipts, and OpenClaw-Govern adds the composition layer that arbitrates across those modules plus async controls.
   - Revise the delegated-remediation paragraph to say the benchmark example is specifically about preserving both the step-up and serialization signals in the trace while the composed verdict is ESCALATE.
2. `paper/07_evaluation.md`
   - Reword Scenario 9’s description and the conflict-resolution paragraph so it highlights the three-part composition: ROMA-style delegation lineage, AARM-style action interception, and async serialization/conflict arbitration.
   - Keep the final verdict and trace-completeness claims unchanged.
3. `docs/related-work.md`
   - Align the matrix row and positioning note for AARM/ROMA so the prose explicitly says OpenClaw-Govern composes delegation, interception, async controls, and conflict arbitration.
4. `team/QUEUE.md`
   - Keep the item as the top unchecked increment, but refresh the date to 2026-07-09 so builder clearly sees this as the next handoff.

# Sources cited
- https://arxiv.org/abs/2602.01848 — accessed 2026-07-09 — ROMA’s recursive delegation and traceable hierarchical execution; useful contrast for delegation structure without governance arbitration.
- https://arxiv.org/abs/2602.09433 — accessed 2026-07-09 — AARM’s runtime interception, contextual evaluation, and tamper-evident receipts; useful contrast for action-layer governance without composition semantics.
- https://arxiv.org/html/2603.16586v1 — accessed 2026-07-09 — Policies-on-paths framing; useful for explaining why runtime path dependence matters, but still narrower than heterogeneous module composition.
- https://arxiv.org/html/2605.05440v1 — accessed 2026-07-09 — Authorization propagation as workflow-level identity governance; useful for delegation/scope attenuation but not full-stack composition.

# Acceptance criteria for builder
1. `paper/08_related_work.md` explicitly states that Scenario 9 is a composition case combining ROMA-style delegation, AARM-style interception, and async conflict arbitration.
2. `paper/07_evaluation.md` still reports `ESCALATE` for Scenario 9 and preserves the trace-completeness claim, but the wording now makes the three-module interaction clear.
3. `docs/related-work.md` no longer leaves AARM or ROMA sounding like standalone precedents; the “OpenClaw-Govern extends…” notes mention composition across delegation, interception, async controls, and arbitration.
4. `team/QUEUE.md` shows this increment as the current top unchecked item for builder handoff.
5. The update does not alter benchmark results, scenario counts, or verdict order; it only sharpens positioning and prose.

# Out of scope
- Adding new benchmark scenarios.
- Changing any code in `experiments/` or `results/`.
- Recomputing metrics or regenerating plots.
- Revising the paper’s core contributions beyond positioning and wording alignment.
