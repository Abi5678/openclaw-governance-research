# Review — 2026-07-15

## Verdict
REQUEST_CHANGES

## Findings
1. **Blocking — paper/summary latency drift.** `paper/07_evaluation.md:52-68` still reports the real-adapter latency row as `0.0077 ms` mean / `+0.0072 ms` overhead for OpenClaw-Ordered, but the current checked-in CSV and regenerated summary output are lower (`results/composition_benchmark_real_adapters.csv:72-81`; `python3.11 results/generate_plots.py` reports `0.0053 ms` mean / `+0.0048 ms` overhead). The paper table is therefore out of sync with the evidence it cites.
2. **Nit — stale summary metadata.** `results/EVALUATION_SUMMARY.md:3` still says `Last updated: 2026-07-06` even though the content was refreshed in this run.
3. **Nit — line-ending noise.** `git diff --check` still warns that `results/composition_benchmark_real_adapters.csv` will be normalized from CRLF to LF on the next Git touch.

## Required fixes
1. Update `paper/07_evaluation.md` so the real-adapter latency narrative and Table 2 match the current benchmark CSV / generated summary, or rerun the benchmark so the CSV matches the paper.

## Verification commands to re-run
- `python3.11 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3.11 results/generate_plots.py`
- `git diff --check`

## Commit / push
- Not committed.
- Not pushed.
