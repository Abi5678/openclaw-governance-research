# Review Increment — 2026-07-03

## Verdict
REQUEST_CHANGES

## Findings
1. `paper/07_evaluation.md:48-66` is stale relative to the regenerated benchmark. It still reports the older OpenClaw-Ordered latency numbers (`0.0055 / 0.0049 / 0.0084`) and says the real-adapter path does not export skip markers, but the current CSV now carries `trace_refs` and the recorded OpenClaw-Ordered latencies are materially higher.
2. `results/EVALUATION_SUMMARY.md:57-69, 133-150` is not synchronized with `results/composition_benchmark_real_adapters.csv`. It repeats the old latency claim and the older validity note, so the paper-facing summary no longer matches the artifact it cites.
3. `results/composition_benchmark_real_adapters.csv:1,65-73` successfully adds `trace_refs` and skip-marker evidence, but the latency data no longer supports the manuscript tables as written. The benchmark evidence and prose need to be regenerated from the same snapshot before this increment can be approved.

## Required fixes
- Regenerate or manually update `paper/07_evaluation.md` and `results/EVALUATION_SUMMARY.md` from the same benchmark snapshot.
- Align the skip-marker wording across paper and summary with the CSV’s new `trace_refs` column.
- If the CSV is the authoritative run, update the latency table and key findings to match it; otherwise rerun the benchmark and refresh the CSV.

## Verification commands for builder
- `python3 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
- `python3 - <<'PY'\nimport csv, statistics\nfrom collections import defaultdict\nrows=list(csv.DictReader(open('results/composition_benchmark_real_adapters.csv', newline='')))\nby=defaultdict(list)\nfor r in rows:\n    by[r['strategy']].append(float(r['latency_ms']))\nvals=by['openclaw_ordered']\nprint('openclaw_ordered mean', round(sum(vals)/len(vals), 4))\nprint('openclaw_ordered median', round(statistics.median(vals), 4))\nprint('openclaw_ordered p95', round(statistics.quantiles(vals, n=20, method='inclusive')[18], 4))\nPY`
- `git diff --check`

## Committed / pushed
- None. Verdict is REQUEST_CHANGES, so no commit or push was performed.
