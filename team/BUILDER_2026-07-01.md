# Builder Increment — 2026-07-01

## Objective
Export per-decision `trace_refs` from the real-adapter composition benchmark so audit-trace / skip-marker claims can be made directly from the tracked CSV artifact, and reflect that in the public evaluation summary.

## Files changed
- `experiments/composition_benchmark_real_adapters.py` — added `trace_refs` collection to benchmark rows and CSV output.
- `results/composition_benchmark_real_adapters.csv` — regenerated benchmark output with the new `trace_refs` column populated.
- `results/EVALUATION_SUMMARY.md` — updated real-adapter wording to note exported `trace_refs` and skip-marker coverage.
- `team/STATE.md` — marked builder run IN_PROGRESS for this increment.

## Validation output
### `python3 experiments/composition_benchmark_real_adapters.py --csv results/composition_benchmark_real_adapters.csv`
```text
Initializing REAL governance adapters...
  ✓ SARC budget adapter initialized
  ✓ AuthZ propagation adapter initialized
  ✓ ROMA delegation adapter initialized
  ✓ Guardrail adapter initialized
  ✓ AsyncFC adapter initialized

none                 accuracy=1/9
sarc_only            accuracy=2/9
authz_only           accuracy=4/9
guardrail_only       accuracy=2/9
roma_only            accuracy=2/9
async_only           accuracy=3/9
naive_composition    accuracy=1/9
openclaw_ordered     accuracy=9/9

Latency overhead (ms per scenario):
  none                 mean=0.0008, median=0.0004, p95=0.0026
  sarc_only            mean=0.0178, median=0.0065, p95=0.0588
  authz_only           mean=0.0032, median=0.0028, p95=0.0053
  guardrail_only       mean=0.0017, median=0.0015, p95=0.0025
  roma_only            mean=0.0019, median=0.0016, p95=0.0028
  async_only           mean=0.0029, median=0.0018, p95=0.0074
  naive_composition    mean=0.0042, median=0.0034, p95=0.0075
  openclaw_ordered     mean=0.0086, median=0.0076, p95=0.0134

wrote_csv: results/composition_benchmark_real_adapters.csv
```

### `git diff --check`
```text
warning: in the working copy of 'results/composition_benchmark_real_adapters.csv', CRLF will be replaced by LF the next time Git touches it
```

## Acceptance criteria check
- [x] Real-adapter benchmark exports per-decision `trace_refs` into the CSV output.
- [x] Regenerated real-adapter benchmark artifact includes the new column.
- [x] Public evaluation summary now states that real-adapter validation exports `trace_refs` and covers skip markers in audit-trace scenarios.
- [x] Local validation ran successfully (`python3 ...real_adapters.py` and `git diff --check`).

## Known issues
- `git diff --check` reported a CRLF→LF warning for `results/composition_benchmark_real_adapters.csv`; it did not fail the check.

## Suggested review focus
- Confirm the new `trace_refs` column is the right shape for direct skip-marker claims in reviewer-facing analysis.
- Sanity-check that the regenerated CSV matches the intended audit-trace behavior for `openclaw_ordered`.
